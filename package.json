{
  "name": "@paulj/payload-fixed-local-auth",
  "version": "0.0.1",
  "description": "A Payload CMS authentication strategy that provides automatic login with a fixed local user",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rimraf dist",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "payload",
    "payload-cms",
    "authentication",
    "auth",
    "strategy"
  ],
  "author": "",
  "license": "MIT",
  "peerDependencies": {
    "payload": ">=3.39.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "payload": "^3.39.1",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  }
}
